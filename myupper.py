szoveg = input("Keremadjon meg egy szoveget ")

def myupper(param):
	nagybetu = ""
	for i in param:
		kord = ord(i)
		if kod >= 97 and kod <=122:
			nagybetu += chr(ord(kod -32)
		else:
			nagybetu += chr(kod)
	break

print(myupper(szoveg))
